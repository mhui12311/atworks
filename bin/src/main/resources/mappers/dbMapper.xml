<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="dbMapper">  <!-- namespace dao에서 정해준거로 사용-->

<select id="selectDb" resultType="com.atworks.hyy.dto.DbDto"> <!-- resultType bean 파일이름위치 --> <!-- id값은 sqlsession에서 정해준값  -->
	select * from master
</select>

 
</mapper>